/**
 * Search message responses for the search api route.
 */
export enum SearchResponseMessage {
  BLANK_NAME_SEARCH = 'Query param "name" is expected to have a value.',
  SEARCH_ERROR = 'An error occurred attempting a search.',
}
